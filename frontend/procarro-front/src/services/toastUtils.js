export const showSuccessToast = (message) => {
  // Implemente com sua biblioteca de toasts preferida
  console.log('Sucesso:', message);
};

export const showErrorToast = (message) => {
  console.error('Erro:', message);
};